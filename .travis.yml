language: node_js

node_js:
  - "node"
  - "10"
  - "11"

sudo: false

git:
  submodules: false

before_script:
  - npm install

script:
  - npm run start-test

after_success:
  - npm run report-coveralls
  - npm run report-codecov
